# PAM-SSW 算法研发约定

本文件适用于本仓库的算法设计、实现、审查、实验和研究计划。目标是构建有数学与物理依据、能够在真实体系上验证的通用 PES 探索方法。用户在当前会话中的明确指令优先于本文件。

## 1. 从问题和原理出发

- 在实现前明确：要解决的科学问题、优化目标、状态空间、自由度、边界条件、约束，以及当前基线的具体失效机制。
- 尽可能从第一性原理、数学推导和已确立的物理规律设计算法。区分严格推导、近似、经验假设和待验证猜想，不把直觉写成定理。
- 禁止为提升个别算例表现而盲目堆叠启发式组件、奖励项、阈值、回退分支或可调参数。优先寻找共同失效原因，简化或替换已有机制。
- 每个新增组件必须说明：现有方法为什么不足、它如何作用于目标、所依赖的假设、额外计算成本、可证伪的预期，以及什么结果会促使我们删除它。
- 启发式确有必要时，必须明确标注其经验性质，解释无法直接推导的原因，并通过对照、消融和跨体系验证证明保留价值。

## 2. 参数必须有来源和可解释性

- 对新增或改变的参数记录定义、单位、作用公式、合理范围和来源；说明它随体系规模、能量/长度尺度、温度、压力或数值精度如何变化。
- 优先采用无量纲形式、可测量的物理尺度、误差控制或有推导依据的自适应关系。自适应规则本身也必须说明假设和引入的参数，不能隐藏调参。
- 区分物理参数、数值精度参数和搜索策略参数。不能将某个体系的拟合值直接宣称为通用默认值。
- 不得凭空给出“最优”参数。默认值须有出处或实验依据；尚无依据时明确标为暂定，并设计敏感性分析。
- 调参与最终评估分开；记录调参预算与选择过程，禁止利用最终测试结果反复调参后仍称为独立验证。

## 3. 文献检索应推动研究计划更新

- 涉及不熟悉或不确定的数学、物理、计算物理、计算化学结论，或准备引入新机制、判断创新性、解释异常结果时，及时进行 web search。
- 优先核验原始论文、原始算法、补充材料、权威文档和可复现源码。搜索摘要或二手介绍仅作为线索，不能代替核对关键定义、公式和适用条件。
- 记录题名、作者、年份、DOI/稳定链接及相关公式或章节；明确哪些结论来自文献，哪些是针对本项目的推论。不能编造引用，无法获取全文时说明证据边界。
- 文献发现改变假设或揭示已有方法时，及时修改仓库内相关研究计划：更新问题、方法选择、对照方案和停止条件，并记录修改依据。不要只追加参考文献而保留已被否定的路线。

## 4. 真实体系端到端验证是科学结论的必要条件

- 单元测试、解析势、有限差分和 toy model 用于检查公式、实现、梯度及接口；它们不能单独证明组件合理、算法有效或具有泛化能力。
- 对组件的有效性判断，必须在与目标应用相符的真实原子体系上做端到端测试：从输入结构、计算后端、候选生成、局部优化、去重和选择，直到最终结构与科学指标。
- 面向“通用 PES 探索”的结论，验证矩阵应覆盖多种相关体系及不同困难，例如团簇、分子构象、周期晶体、表面/吸附体系，以及不同尺寸、组分和势能面特征。具体选择由待检验假设决定，不机械要求每次改动重跑所有类别。
- LJ 等模型势基准是补充证据，不能代替真实材料/分子体系。明确真实体系所用模型的适用域；使用 MLIP 的结果不自动等同于第一性原理验证。
- 必须包含当前稳定基线、必要的成熟方法对照以及能够隔离组件贡献的消融。采用可比的初态、后端精度、终止条件、随机种子组和计算预算；报告重复实验的离散程度与失败案例。
- 按总成本评估收益：记录能量/力/应力调用、失败淬火、初始化、回退和其他实质开销，并同时报告适当的实际耗时。不能用更多计算换来的改善冒充算法效率提升。
- 根据目标预先定义成功指标，例如有效且不同的极小值覆盖、低能结构发现概率、达到目标的成本或结构转变发现率；不能仅用接受率、archive 大小或测试通过数代替科学效果。
- 独立检查最终结构的能量、力、允许应力和约束，并检查与任务相关的化学/物理合理性。小梯度并不保证目标意义下的稳定结构；解离平台、晶胞膨胀/坍缩和越出模型适用域必须单独诊断。
- 真实体系验证尚未完成时，可以交付实现和数值检查，但必须标为“实验性、科学有效性待验证”，不得升级为通用默认策略或宣称生产验证完成。

## 5. 保持目标函数和几何的一致性

- 明确区分全局能量优化、PES 覆盖、动力学路径发现和统计系综采样；某一目标的改进不能直接作为另一目标的证据。
- 能量、力、应力、偏置和优化坐标必须保持一致的符号、单位、链式法则与约束投影。变胞时明确坐标约定、原子/应变度量、外压项和允许晶胞自由度。
- 区分“固定晶胞逃逸后变胞淬火”和“应变参与逃逸的联合变胞搜索”，不能混称。不能从局部优化收敛推断全局搜索质量、相稳定性或正确采样分布。
- 极小值身份不能仅由能量判断；结构比较须考虑适用的组分、周期边界、对称性和原子置换，并明确近似去重的限制。

## 6. 研究记录与交付

- 对实质算法改动，在仓库文档中留下简洁证据链：问题 → 原理/文献 → 假设与参数 → 最小实现 → 对照实验 → 结果与局限 → 保留、修改或删除决定。
- 保存可复现实验所需的代码版本、完整配置、结构与模型来源、随机种子、预算、运行状态和结果路径。结论必须能追溯到实际产物；计划、作业结束、数值收敛和科学验证分别报告。
- 分支角色以实际 Git 历史和验证产物为准，不以名字或旧记录判断。实验在独立工作分支开展，保留稳定基线和无关工作区内容。
- 本文件规定验证标准，不自动授权新增长时间计算、GPU/HPC 作业或破坏性清理。按用户已授权范围推进；资源或数据不足时先完成可执行方案并明确剩余证据，不降低科学结论标准。
