# Historical mainline; superseded by MAINLINE.md

# 当前主线：完整独立 SSW 家族与可验证的算法收益

## 最新用户指令覆盖此前的“扩展后置”顺序

用户要求尽可能完整实现各种SSW变种本体，持续用反编译和算法原理寻找可验证收益。
因此SSW/VC仍作为公共核心，LS/RC/GA的真实缺口现在并行推进。旧文中的“LS/RC/GA后置”
仅记载当时决策，不再阻止当前开发。常规资源安排已授权自行处理；已冻结的8例16k请求
测试保持原参数、原预算，后续开发不污染当前分母。有限实验不能证明不存在任何进一步
改进；保留失败/负面证据，并以同预算真实体系对照决定留存设计。

已新增周期完整image键LS、联合VC-LS应力/生命周期、单链RC坐标与独立搜索；native LS
算术/周期状态控制已独立实现，上层caller约定继续显式核对。GA周期算子、多链RC等
缺口按可独立检查的组件推进，不用模块名或接口存在冒称整个家族已完整复现。

## 当前依据：算法原理、可验证的原版行为与真实体系证据

用户明确：论文可能仅作参考，核心是算法思想和反编译结果。工作以可检验的目标、
坐标/力一致性及原版实际数据流为依据；论文用于解释与补充，不要求照搬其记号、
印刷错误或全部经验参数。复现原版行为与设计独立替代分别标注，不把后者冒称逐指令
等价，也不因尚有native字段未闭合停止可独立验证的实现。

## 用户提供2014全文后的结构认识

已取得并核验12页出版社全文，DOI10.1039/c4cp01485e。§2.2/2.3确定原算法为
CBD-cell循环（胞软方向、线性胞位移、有限步固定胞原子松弛）→按步调度固定胞SSW
→全自由度真势淬火→MC。当前统一3N+6 log-strain walker是独立扩展，不能标作2014复现。
此前Cu/AlOH/TiO2的梯度与真实体系结果仍有效，其科学范围限于该独立扩展。

下一步顺序据此修正：
1. 定义固定分数坐标下的ASE胞焓梯度、九胞向量中的旋转投影，先闭合公式/符号。
2. 增加独立CBD-cell分块，不用统一joint方向代替；原文有限步atomic relax允许未完全收敛。
3. 将已有atomic SSW climbing从完整run_ssw生命周期中分离，以免嵌套调用引入额外初始
   淬火、提前MC或错误能量参考；最终joint true quench与MC只由晶体外层组织。
4. 用真实小胞与原始案例核验分块实现；将它加入对照，才讨论与independent joint的差别。

正文同时存在Eq7符号、Scheme1/正文分支相反与HCell计数歧义，必须保留证据并用SI或
有界native数据流核对；不新增一组未解释的经验开关。SI仍缺。已有三臂长算未获批准、
未启动；其原配置仅比较独立变体，缺少2014分块基线，暂不作为原版复现主验证。
详细页码、公式与PDF视觉核对见 `vc2014-native-crosscheck.md`。


用户2026-09-10明确纠正优先级：总体目标仍是独立ASE兼容的SSW系列，但先完成可复用的
SSW内核和VC-SSW，再考虑LS/RC/GA。之前把GA闭环提前不是后续工作的依据。
LS/RC改变局部搜索或坐标表示，GA主要改变外层调度；均后置，不因已有代码继续扩展。

## 当前执行顺序

1. **SSW内核完整性审查与补齐**：明确真实目标/偏置目标、软模方向、连续climbing、
   biased quench、true quench、接受/失败状态、刚体及周期几何。Safe-total维持工作数值后端。
   逐项区分论文一致实现、原版行为证据、数值替代与真正缺失；不能以完成局部优化器对照
   声称整个SSW内核已经完整。反编译只解决这些组件的具体阻塞问题。
2. **固定胞周期SSW**：闭合PBC坐标连续性、minimum image/展开坐标、平移规范、
   方向与Gaussian的能量-力一致性，以真实周期体系端到端验证。
3. **联合VC-SSW**：从原子与应变联合坐标/度量出发，明确E+pV及应力链式法则，
   cell参与随机方向、soft-mode rotation、偏置climbing、局部优化及true quench。
   消除纯晶胞旋转/等价表示，说明体积与剪切自由度及数值域。只能使用提供相应
   energy/forces/stress的ASE Calculator；不把末端cell-relax冒称VC-SSW。
4. **再评估扩展**：LS/RC通过真实需求决定；GA优先评估现成ASE-GA/USPEX能否复用。
   Java实现作为原版行为参考，不预设优于其他GA，也不预设必须继续移植。

## 研发与验证边界

禁止盲目增加启发式/参数；物理度量和参数需推导或来源。真实体系E2E是算法结论的
必要条件，有限差分与单元测试只验证实现。保留全部失败和成本，小力证书与稳定极小值、
完整分子/碎片身份分别记录。不得因“能接calculator”宣称所有物理域均已支持。

正式搜索独立Python，不依赖LASP/JAR，不调用PAM walker；已有PAM数值优化器可复用。
保持稳定生产分支；无HPC新提交。本轮停止LS/RC/GA新增开发和性能campaign。

## 已完成工作保留为证据，不决定优先级

- Safe-total与原机器码内核31项对照：31/31 vs29/31；不是完整BFGSDRIVER排名。
- Cu13独立SSW：Safe失败1/60、32517请求，14严格结构组；未接受跨组迁移。
- C4H6/GFN2：LS响应接近目标，但更多成本未见完整分子覆盖提升，部分小力落点为碎片。
- TYPE0 GA短闭环已实验性跑通，修正原版重插碰撞异常；尚无等成本优越性证据。

旧计划和详细阶段结果归档在2026-09-10-mainline-before-kernel-first.md；
本文件的优先级覆盖旧计划，不继续执行旧顺序。

## 2026-09-10 执行进展：PBC贯通，联合VC进入真实体系检查

- 固定胞Cu8/EMT两seed×Ritz/dimer共20步、24个独立真力检查已通过；没有发现新相的证据。
- 联合VC几何、flat Safe-total、flat dimer已经实现；原子与应变共同进入全部escape链路。
  梯度使用完整Frechet导数和E+pV，非末端cell-relax。L显式传入，无通用最优值宣称。
- Cu4/EMT三个连续VC步骤两次合格落点、一次真淬火失败，失败保留当前状态；正在诊断，
  不以提高预算或新增回退掩盖。AlOH26/MACE已完成E/F/stress预检，端到端检查继续。
- 已获取2017 SSW-NN官方SI的24套TiO2结构（TS单列）及Cambridge LJ38/75参考。
  上传example无C60/PdO/CuO实际坐标，AlOH26为最直接的上传VC输入。
- 2014 VC-SSW全文现已取得；SI仍缺。全文下载细节见本文件顶部及vc2014-native-crosscheck.md。

接下来只处理SSW/VC失败诊断、物理认证、有限应变度量敏感性与有界真实体系验证。
生产campaign需先明确等成本预算、结构/相身份及成功标准；LS/RC/GA继续后置。

### 独立物理资格已推进（补充，覆盖上面的进行中状态）

Cu失败已定位并修复为accepted-q的物理停止准则；独立严格认证的两个seed共8个minimum
均有正joint Hessian，六个后续落点的邻居壳层/体积与独立hcp参考一致。AlOH26完成
完整一步搜索，质子所属O发生改变；高焓落点被MC拒绝但保留，始末有限胞Hessian均正。
TiO2/rutile最新内核完成一步并接受降低0.122eV的落点；始末joint Hessian均正。
结构匹配将其与当前rutile/anatase/TiO2-B参考区分，暂称未归属候选，不命名新相。
这些是真实体系的有限胞、当前势模型证据；仍没有等成本搜索优势或原版VC逐式复现结论。

当前准备三臂同预算脚本：fixed / posterior-cell-quench / joint-VC。不得把局部正曲率、
单步降能和一次化学配位变化当成泛化性能。长campaign启动前给出可复核命令、明确预算
与结构判据，保留独立初始化与失败成本。原2014 SI缺口及native数据流未闭合项继续单独记录。

### 用户进一步澄清：机制、native证据与复杂案例优先

论文是参考而非唯一规范。保持统一joint与cell/atomic block两条明确的候选实现，
不为复现印刷歧义增加一组兼容开关。现在已提取纯atomic_climb、共享pure cell_quench，
新增CellChart/胞方向及分块outer；native stress2dedlatt原指令公式已闭合，其他字段符号
和方向跨cycle继承仍明确待核。详见block-ssw-design.md及native-cell-force-contract.md。
复杂体系主验证转向AlOH26和TiO2_48(brookite/phase87/139)，小Cu仅用于公式与接口检查。
原有小Cu长算计划继续保持未启动；未批准的长期资源不会因这次实现更新而自动消耗。

### 复杂体系阶段进展与接下来判据

胞/原子分块已贯通，180项独立模块测试通过、1项跳过；checkpoint与block专项7项通过。
这些是实现检查。AlOH26的组合阶段通过边界重放完成：385次原子climb+80次joint quench，
另1次fresh证书，累计原始与重放1578次搜索+3次fresh。相对初态高0.48810eV，结构与
前一cell-only落点不同；不追加MC，不改写原两步实验中的预算截断。完整几何诊断见
block-complex-material-replay-geometry.md。Brookite48重放达到600.473s上限，新增1092次搜索，累计1701次搜索+2次fresh。完成9个Gaussian，第10个biased quench截断，无最终落点，不计成功。

下一步先区分“产生化学/配位变化”和“同预算带来有效覆盖/降能收益”。不因单例高能或
体积变化直接修改cell步长、堆叠回退；用已准备的复杂体系配对对照协议判断。原版信息
优先补与坐标、力和调度相关的窄数据流；论文印刷歧义不再触发兼容分支。

### 核心近似的可证伪问题

见block-core-review.md：当前胞mode是固定分数坐标的曲率问题，随后有限步原子松弛，
不能将其称为原子已充分松弛后的有效胞Hessian。稳定stationary条件下后者含原子-胞
耦合的Schur补；两者差别属于proposal近似，不是当前链式法则错误。四臂实验首先检验
整个胞模块相对posterior-cell-quench是否增加单位成本下的可信结构覆盖；它本身不能
隔离fresh方向、步长或无胞Gaussian的单独贡献。未出现证据前不增加补偿项或调参开关。

### 下一次计算已准备，未启动

compare_material_arms.py已支持四臂完整生命周期，block/joint整链执行，避免重置
atomic_period。新增EMT wiring测试保留真实失败，总182项通过、1项跳过。冻结代码、
配置、实际AlOH26/brookite48输入及8条命令见
research/ga_ssw/prospective/complex-vc-feasibility/manifest.json。每臂2步、seed17、
上限2000请求，合计16000请求、单CPU串行搜索上限3h20m（模型加载另计）。
此为需要批准的下一次可行性计算，不是已执行验证，不授权后续168k性能campaign。
