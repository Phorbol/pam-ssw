# 随机 C60 的固定预算延长对照（预注册开发协议）

问题：短程测试尚未形成完整笼。增加相同势面调用预算后，真实 LASP 与独立 Python SSW 是否持续发现更低能、拓扑更接近富勒烯的合格结构？

竞争解释：原先6000次调用不足以完成必要重排；或独立实现的逃逸/搜索组织仍有实质缺口。
能量随成本持续改善支持继续投入搜索，但不能单独证明成笼或排除机制缺口。
若双方长期停滞，下一步应定位结构重排瓶颈，不能直接再增预算；若原版稳定成笼而Python不成笼，优先核对缺失机制。

## 冻结方案

- 原有随机碳云输入 seed17093、17094；输入协议不变。它们已参与开发，不称独立验证。
- 两臂：真实 LASP external→MACE；Python strict SSW + 显式 NativeMCSettings(.1,99999)。
- 同一 MACE-OMAT-0-small、CUDA float64、明确记录确定性设置。原版实际内部随机种子从输出保存，不称两臂随机流相同。
- 每臂每起点最多60000次搜索E/F，初始淬火计入；最多1000外步。预定6k/15k/30k/60k成本截面，截面取当时已完成且合格的落点，禁止截面后续信息回填。
- Python每条wall3600秒；原版外部脚本启动开销较大，每条wall7200秒。触发wall时明确计为资源截断，未达等调用预算的截面不作等成本比较。
- 普通Gaussian、旋转阈值、Safe-total/history500、内层/外层收敛、原版重连/压缩沿用原配置。无扫描、无中途调参或重启挑选。
- 能量参照 -514.5727735075726 eV，操作容差0.01 eV；公共最大原子力范数≤.03 eV/Å；结构图用既有1.64/1.7/1.8 Å三阈值。图笼候选需再核验三维物理结构。
- 独立复核及原版必要公共淬火费用单列，不隐藏在搜索预算之外的成本。

## 边界与停止

这是两条开发初态上的预算曲线，不是独立成功率估计。Python与原版仍有方向、内层阶段终止、重连/压缩及外层覆盖差异，不能把整体差距归因到单一MC规则。
每次运行保存原始轨迹、全部返回落点、失败、终态及请求账本。达到上限或真实算法错误即停止该运行，不自动追加预算。
总搜索上限240000 E/F；最多两张V100并行，Python作业最多约2小时、原版最多约4小时（含两起点），整体GPU墙时上限约6小时；实际费用/调用与复核分列。
本协议尚待准备产物核验后执行，不以文档存在代表已运行。

## 提交前核对与实际运行

发现旧 extxyz 与 native ARC 的坐标精度不同，最大差5.0000004e-9 Å，
未通过1e-9 Å预检。未放宽阈值：Python改为读取相同native ARC坐标并以traj无损保存，
pbc按既有external MACE非周期契约设置。两侧实际提交输入坐标逐位相同。
旧输入、旧plan及失败检查均保留在Python证据目录，prepared.json定位实际traj。
该差异提示旧跨方法比较不能声称坐标逐位相同，但不能据此解释全部旧能量差。

Python job1383788、真实native job1383704。证据分别 c60-fixed-budget-python-20260918、
c60-fixed-budget-native-20260918。提交不代表数值或物理验收完成。

用户随后明确：不要研究远低于机器学习势精度的差异。上述序列化统一仅作为已完成的输入准备，
不将5e-9 Å差异视为物理失效原因，不追加精度实验；算法判断以实际搜索收益和结构目标为准。
原版独立复核job1383835依赖1383704结束后执行，最多2002单点、10分钟，无额外局部优化。

第一条Python已完成预定60000请求（末次拒绝触发request_cap，不是模型错误），
54个完成外步、含初态55个返回结构均独立复核合格，笼与能量指标均未达标。
6k/15k/30k/60k最低能量为-483.236358/-494.319750/-496.073730/-496.576185 eV。
解析器按initial成本+每个完整outer record总成本定位落点，没有只累计真实quench成本；
第二条与native尚未完成，不能汇总为全系列结论。
原版启发高度机制的后续隔离见2026-09-18-c60-native-height-control.md，job1384136依赖Python基线结束。

## Python基线两起点完成

job1383788 COMPLETED 0:0，46分01秒，共120000搜索+116独立复核，116/116资格合格。
每条在60000调用上限退出；底层evaluation_failed由研究预算拒绝造成，不能计成MACE异常。

| seed | 6k | 15k | 30k | 60k | 完成外步 | 返回结构/复核合格 | 笼/能量目标 |
|---|---:|---:|---:|---:|---:|---:|---|
|17093|-483.236358|-494.319750|-496.073730|-496.576185|54|55/55|均未达到|
|17094|-475.000214|-487.900869|-495.706862|-502.514172|60|61/61|均未达到|

单位eV，完整派生结果见Python目录python-analysis.json。
6k到60k改进13.34/27.51 eV，证实原小预算不足以评价充分搜索效果；
第一条后半程仅降.50 eV，但第二条后半程降6.81 eV，不能把局部平台概括为共同失效。
两条均未形成图笼候选，也未达到参考能量，不能声称C60验收通过或估计一般成功概率。
原版和高度对照仍在运行，当前不作完整方法排序。
