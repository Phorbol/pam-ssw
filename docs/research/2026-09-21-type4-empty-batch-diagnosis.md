# TYPE4 表面 GA 空批次：诊断与修复决定

目的：补齐既有固定基底 TYPE4 GA 与 LS-SSW 的真实子代接口证据，不评估 GA 搜索性能。

CPU1435094 在 Cu7Ag/EMT、FixAtoms-only 条件下完成873次搜索请求、8次独立复核；8/8复核通过，但没有子代，原验收明确未通过。LS参数是诊断设置，不是Cu/Ag物理参数表。原始产物：[summary](../../../ga-ls-composition/research/ga_ssw/evidence/type4-cuag-ls-offspring-20260921/summary.json)。

竞争解释原为父代选择不足、候选被碰撞过滤、算子没有实际执行。日志显示6个父代、生成0、过滤0、异常0、辅助调用0；源码 `propose_type4` 按 G//4 分配交叉/重载/扰动/重建，G//8 分配重构。原协议沿用模拟接口测试的 G=1，必然不执行任何有效算子。此为协议配置缺陷和输入校验缺口，不能解释为物理搜索失败。

决定：保持整数分配规则不变；原生 proposal 拒绝 G<4，控制器仅在启用原生 generation 时于 quick walks 前拒绝此配置。自定义 proposal 和 generation=0 不应受影响。G=4允许部分算子，G=8才首次包含全部五类。针对性回归先验证现有代码失败，再验证局部修复。

新真实验收建立独立v2运行标识，用G=8；父代、模型、LS、种子及6000搜索/20独立复核预算保持不变。原881次总请求保留计账，不覆盖原产物。新协议仍可能碰撞拒绝、预算截断或资格失败，届时按原判据报告，不调参数制造通过。

当前状态：两处校验及两测试文件已集成（未提交/发布）。CPU1435211复现原版两项失败；1435258相关16项通过。早先1435153受pytest路径注入影响、1435211全套阶段缺相对fixture，属于验证打包错误，日志保留，不作为核心失败。v2 CPU1435269已提交，尚未获得真实子代新证据。C60长预算旋转对照独立运行，固定胞SSW仍是最高优先级；VC/RC及新GA机制暂缓。
