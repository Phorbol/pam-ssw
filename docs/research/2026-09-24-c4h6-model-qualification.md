# 下一项有界实验：C₄H₆模型资格，而非搜索排名

状态：用户于2026-09-24明确批准C₄H₆使用现有MH-1/omol进行有界资格检查。
已完成：首尝试GPU1469300在零E/F时拒绝错误的周期坐标选项；修正后的GPU1469348
退出0，三结构均通过预定数值与连接图资格，总26搜索请求+3独立复核。
本文不改搜索或模型默认；此前待模型选择状态保留在Git历史中。

问题：所选势是否至少保留论文中三个闭壳层结构的局部稳定连接关系，足以支持下一轮
SSW/LS反应覆盖开发对照？不要求它数值复现PBE，也不把训练域标签当作反应势准确性证明。

输入固定为ASE G2的 `butadiene`、`cyclobutene`、`bicyclobutane`，对应论文A/D/E的
结构类别。保存G2原始坐标与ASE版本；它们不是作者PBE优化坐标。不使用旧GFN2搜索里
按低能选出的结构，不引入未明确电子态的自由基产品，也不对资格结果不利的输入重抽样。
论文B的顺式构象先补报旧轨迹二面角；人工旋转所得几何不能假称其真实局部极小值。

复用当前公开 `run_ssw(steps=0)` 初淬火路径，Safe-total/history500，真实力阈值
0.03 eV/Å、最多400优化步；每输入最多1500 E/F请求、120秒。三输入合计4500请求，
另各1次独立计算器E/F复核，总4503调用上限；单V100最多10分钟，无全局搜索或LS。
阈值与优化设置沿用当前有界资格流程，不以本次结果调参。失败保留端点与全部成本。

独立检查能量/力、组分、非周期边界、晶胞以及前后元素成键图。必须保存所有端点，
无论连接图是否变化。连接图沿用旧H/C显式表，只作结构筛查，不证明电子态/键级。
报告三结构相对能量及与论文排序的差异，不把强行得到论文能序当数值收敛条件。

预期的两种区分信号：

- 三类均保留且力合格：允许设计冻结的SSW/LS覆盖开发实验；仍不证明势垒、自由基、
  解离或整个反应空间可信。第一轮评价范围须明确，不冒充论文完整A–J复现。
- 结构类别坍缩、非有限能量/力或预算内不能淬火：停止在此模型上扩大该搜索，记录模型/
  数值层解释，不加大LS强度或改变SSW参数以掩盖问题。

是否开展完整A–J反应验证，以及是否需要小规模PBE参照，留待上述证据后决定；本资格
检查不自动授权新DFT项目。原文：Guan、Shang、Liu，JCTC2024，§3.1/Fig.5，
DOI [10.1021/acs.jctc.4c01081](https://doi.org/10.1021/acs.jctc.4c01081)。

## 准备与验证状态

脚本、固定配置与10分钟GPU脚本位于
`research/ga_ssw/evidence/c4h6-model-qualification-20260924/`。
入口在模型加载前检查计划状态；获批后改为 `ready_for_qualification`。
获批前实际运行不带执行选项的入口得到 `prepared_not_executed`，退出0；仅标准库路径执行，
未导入数值后端或调用模型。`bash -n gpu.sbatch` 和 `git diff --check` 通过。
准备中一次f-string语法错误已修正并复查；这不是模型或算法失败。
上述是提交前检查记录；实际MACE结果如下。

## 实际结果与下一决定

首尝试1469300（26秒，退出1）误用周期专用 `translation_only`，在任何E/F前被拒绝。
原输入/配置/失败结果完整保留。新目录 `c4h6-model-qualification-20260924-r2` 中仅把
该配置改为非周期 `direction_only`，其余参数、三个输入、预算不变；1469348完成于19秒。
两次作业合计45秒，真正E/F调用29次；没有为获得通过而放宽阈值或更换输入。

| ASE G2输入 | 搜索/独立调用 | fresh最大力/eV Å⁻¹ | 相对丁二烯能量/eV | 原图保留 |
|---|---:|---:|---:|---|
| trans-butadiene | 9/1 | 0.02529 | 0 | 是 |
| cyclobutene | 7/1 | 0.01827 | 0.40348 | 是 |
| bicyclobutane | 10/1 | 0.01316 | 1.04558 | 是 |

三者fresh能量与保存能量差均为0，组分/原子顺序/胞/非周期状态均保持，账本与结果请求数
逐案一致。独立只读审查重新计数原始请求行，确认26+3，无拒绝；连接图判据仍是既有
元素距离阈值图，不声称独立电子结构确认。未做Hessian/振动，故只称力合格结构，
不证明严格极小值、反应势垒、自由基/解离精度或论文PBE等价。
[实际结果](../../research/ga_ssw/evidence/c4h6-model-qualification-20260924-r2/qualification.json)。

按事先决策继续准备三策略完整外步的费用/执行链试验，不调整参数：SSW、论文LS、
native LS各12步、最多12000请求，单seed59，仅用于确定后续400步对照预算和发现
执行阶段故障，不用于方法排名。另逐极小值最多13个fresh/臂。原始随机流相同不意味着
三种算法逐步消耗相同随机数；共同起点/种子用于可复现，不虚称轨迹严格配对。
见[固定协议](../../research/ga_ssw/evidence/c4h6-mh1-lifecycle-20260924/plan.md)。

模型来源补查（2026-09-24）：MACE维护者在
[官方讨论1462](https://github.com/ACEsuit/mace/discussions/1462)更正了MH-1包含RGD1训练的旧描述，
并表示预期omol适合反应任务。后者是维护者判断，不是本例反应精度验证；本研究不据旧模型卡
宣称已包含特定反应训练集。使用已冻结的模型文件和head，不在这轮实验中升级或替换模型。
