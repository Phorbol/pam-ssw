# 缺口驱动研发与论文级案例验收

状态：用户明确要求按算法/功能缺口开发，并且每项功能用论文级案例驱动测试。
基线ba5bca6。取代仅按短实验收益暂缓功能完整性的优先级；不要求逐位复刻LASP。

## 关闭规则

每项分别记录：原理/原版证据、已实现代码、未闭合语义、实现检查、论文级案例
端到端结果、效果边界。单元测试通过、数步落地或短对照收益混合不能关闭功能。
功能完整但没有效率优势可以交付为可选实现；明确不升级默认。不得为了证明效益
追调单个体系。真实案例的模型、边界条件和目标变化必须公开，不能称原论文数值复现。

## 当前顺序及验收病例

|顺序/缺口|现有实现与缺失|案例驱动与验收|
|---|---|---|
|P1 周期固定胞完整局部方向/记忆|自由簇c1/c4/c6/c9和受限非周期已实现；周期选轴、局部映像、跨胞记忆与恢复未接通|文献来源TiO2 rutile/anatase，以及较大brookite48作为开发面板；OMAT-small。同一结构合法晶格平移表示必须给出等价方向与能量/力；完整多Gaussian、连续/恢复、落点资格；再以相同预算比较现有全局方向的有效独立极小值和成本。固定胞不声称复现变胞相变。slab需单独来源及固定基底资格，不以Cu13替代|
|P2 LS未闭合调用语义与周期完整性|论文势与反馈、native表算术及周期save/zero/restore均已实现；原版拒绝路径几何指针、部分元素表/镜像身份未闭合|C60缺陷重排和随机成笼(MH-1/omol)、周期TiO2(OMAT-small)；Fe7C3原论文hcp逃逸列后续，先核对自由度/表来源/模型。验证真实响应、冻结邻居、多外步强度与拒绝后状态；不能把低力或响应接近目标当有效探索|
|P3 完整描述符梯度方向|S1值/解析梯度为研究原语；S1-S6族、选择器、周期映像与walker未完成|先在上述C60和TiO2结构核对完整中心/邻居梯度及实际触发；同一后端同预算Q开/关，记目标结构事件/覆盖/成本。MACE特征池评分不替代梯度方向|
|P4 VC/RC|block/joint/RC-VC可跑；native生命周期差异及同等恢复/池能力缺口保留|VC论文相关SiO2/TiO2/作者AlOH，RC论文XXXII172显式拓扑；配平允许自由度、压力与终态淬火；不以toy梯度替代论文场景|
|P5 GA|独立工作流已实现；Java描述符不等于论文DCCD|后续多funnel案例，同预算对比池重启和ASE GA；缺失公式先核实，不抄常数替代设计|

P1病例是文献关联材料功能/机制面板，不是3种TiO2即证明通用性。后续泛化必须
增加不同组分/配位；不机械一次跑全矩阵。短有界probe先估每个完整逃逸的成本与
目标事件可观测性，再冻结重复数/请求上限；不写任意巨额预算，也不根据中间能量加码。
每个新功能的目标案例需在实现前落实输入、结构指标和对照。

## 当前核实的证据与产物

- SSW/LS/VC/RC原文来源：`research/ga_ssw/evidence/mainline-reassessment-20260923/source-ledger.csv`。
- TiO2来自DOI10.1039/c7sc01459g的SI；旧3外步为资格，不是效果。
  原产物位于相邻ga-ssw-behavior-parity/research/ga_ssw/evidence/native-ls-tio2-lifecycle-20260920。
  本checkout同名相对路径缺失，须使用真实位置，不能把旧文档链接当可运行输入。
- brookite48源位置由本checkout `research/ga_ssw/evidence/periodic-rotation-priority-20260923/plan.json`记录；
  与作者AlOH的旧旋转对照不能代替新局部方向试验。
- 原版c1范围距离与c4成键分组确有周期距离指令；c6映像和阶段位移的周期调用语义未闭合。
  见native-global-random-workspace-audit、2026-09-17-c4-group-route-review及native-displacement-boundary。
- LS完整cycle.advance已调用；normal_update helper旧注释不能当周期状态未实现证据。
- 只读审查分工：Luna周期方向证据、Luna LS算术/调用差距；主agent负责设计、案例目标与整合。

下一步是P1周期几何契约设计和原版分支核对，尚未提交新的PES作业或改默认。
