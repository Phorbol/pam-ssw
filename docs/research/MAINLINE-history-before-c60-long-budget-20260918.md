# 历史主线快照（不作为当前任务入口）

当前入口为 [MAINLINE.md](MAINLINE.md)。以下保留阶段迁移前原文，不回写旧决定。

# 当前主线：先交付可靠的固定胞 SSW / LS

## 目标与阶段

构建独立Python/ASE的SSW系列，吸收LASP有用设计，并在真实体系上证明搜索价值。
目前核心流程可运行，处于参考实现收拢与效果验证阶段，尚非生产级完整复现。
优先顺序为SSW → LS → GA；VC/RC保留实验实现，暂不占主线。
后续研究后端按用户决定固定MACE-OMAT-0-small、CUDA float64，通用ASE接口保留。

## 当前优先验收：随机 C60 与真实 LASP 同势面对照

下一阶段已启动：相同随机碳云坐标，两起点×Python显式native-MC/真实LASP，
每条60000 E/F上限，预定6k/15k/30k/60k预算曲线。job1383788/1383704，
无参数扫描，不称独立成功率。提交前修正旧extxyz/ARC精度差，两侧实际输入逐位相同，保留失败预检。
[固定预算协议与限制](2026-09-18-c60-fixed-budget.md)。
第一条Python60000请求已完成：55/55复核合格，仍未成笼/能量达标，后30k仅降约.50 eV；
其余轨迹进行中。已排队单因素原版启发高度对照1384136，新增最多120000搜索，LS暂不启动。
[高度策略对照及为何先于LS](2026-09-18-c60-native-height-control.md)。

当前推进：已通过确定性设置下四体系首个完整外步的跨进程重复检查，四对 E/F 账本和结果完全相同。
job1383430，7154 搜索+16复核；此前单点48调用检查用于区分调用次序影响，未宣称任意硬件位级一致。
独立 native MC 已作为显式可选模式接入共享 SSW/LS 驱动，包含每walker状态、随机数消费和检查点恢复；
普通 Metropolis 与旧检查点保持兼容。63项核心/检查点/LS针对性检查通过；job1383607四体系接入验证完成，15581搜索+20复核，
16次决策离线重建一致，20/20数值资格合格。C60仍未成笼或达到参考能量，不改变默认。
[MC 接入契约与重复基线](2026-09-18-native-mc-integration.md)。

2026-09-18 新优先问题：相同的 `150 K` 并不代表相同的 MC 接受概率。
已恢复的 native MC 有固定能量差除数 20；发现时尚未接入主驱动，现已完成显式接入，见上。
使用现有普通 Metropolis、仅换算为 2999.8444238 K 的四体系对照已完成 1382746/1382747，
24000 搜索加 26 次复核，全部数值合格；但首次 MC 前已分叉，温度效应不可由终值归因。
初始力有 1e-15～1e-14 的差异；两 C60×两温度的固定 E/F 首步重放已吻合旧基线，
确认首次分叉并非温度造成。暂缓参数扫描，下一优先是 MC 接入契约及重复基线的可信比较。
仅温度换算的旧对照未复制原版随机数消耗、NSAME 和外层覆盖行为；新接口包含前两项，外层覆盖仍不在范围。
不再把 Gaussian 中心或整段 quench 入口当作原版每次反向通信入口来实现回退。
原随机 C60 诊断只有个位数至数十个完成外步；论文万级外步不能与 6000 E/F 混称。
[接受尺度、状态恢复边界与后续验收预算](2026-09-18-mc-selection-semantics.md)。

最新：阶段控制接口真实检查发现研究 monitor 对 Gaussian 力重复计数（数组别名），已用独立缓存修复。
10 项针对性检查和实际 Gaussian 力/门限复核通过，旧阶段控制报告已更正；下述 24 条无 adapter 对照不受影响。
修复版四真实起点对照已完成：job1378698/1378699，24000 搜索+27复核；27帧数值合格，420门控快照重建吻合。
C60 两起点相对 strict 分别高7.454/低8.440 eV，water15 高.344 eV；无一致收益，不升级默认。
冻结目录 evaluated-stage-gates-v2-20260918；C60 笼/参考能量目标仍未达到。
真实二进制 GDB 观察在交互与计算节点都被 ptrace 限制阻断；不修改权限，暂以已评估状态上的可移植门控对照推进。
[问题、修复、固定协议与复现边界](2026-09-18-evaluated-stage-gates.md)。

当前推进：旋转阈值 × 偏置阶段上限两因素诊断已完成，覆盖两个 C60、Cu55、水团簇四起点。
job1378078/1378079 均完成，96000 搜索请求加 147 复核，147 帧全部通过共同数值资格检查。
单独放宽旋转在两 C60 起点均更差；偏置 cap15 一好一坏，省下的偏置请求转移到了旋转/真实淬火。
不调整默认、不继续扫描这批种子的数值参数。9 项阶段预算检查通过；cap15 不等价原生阶段计数。
既有 PAM Gaussian 高度/高度宽度消融也已完成，job1378191/1378192：48000 搜索加 51 复核，全部合格。
两个 C60 与水的两种 PAM 变体均未超过原 strict 基线；Cu55 无实质低能收益。
664 个 Gaussian 代数曲率设定符合公式且无高度截断，但这不证明真实 Hessian 或逃逸有效。
6 项已有 PAM 检查通过，无新核心算法改动。停止围绕本批起点扫描参数，不升级默认。
下一优先级：原生固定胞 escape 的阶段停止、状态恢复、Gaussian 历史与真实淬火衔接，
先用既有适配边界核验，不新增公共架构；完整 PAM/原版总体优劣仍未解决。
两组均是已淬火起点的开发诊断，不替代随机碳云正式验收，不作普遍方法排名。
[固定协议、竞争解释与决策边界](2026-09-18-ssw-allocation-diagnostic.md)。

2026-09-18阶段结论：真实原版+MACE与Python在两个随机C60初态的开发对照已完成。
原版得到更低的合格落点，但所有配置均未达到完整笼/参考能量目标，不能据两种子作普遍排名。
已新增默认关闭的c1逐原子mask修正：两条恢复轨迹由2092/2072请求处提前失败变为均跑满6000预算，
解决定义域阻塞但未追平原版。用户质疑后明确：有限差分、旋转预算、偏置停止流程均未对齐，
优先核对参数定义及预算分配，不能直接归因为算法机制；再隔离偏置策略与碎片处理。
本批未覆盖PAM自适应Gaussian；它应作为下一项显式对照，不能用本批结果否定所有PAM策略。
[完整结果、数学定义与下一决定](2026-09-18-c60-first-native-comparison.md)。

用户确认同时报告完整富勒烯笼形成率、达到同一模型 C60 参考能量的成功率及交集。
真实 LASP external 接口接入常驻 ASE/MACE 已完成九次请求验证，三进程正常退出；
能量/力符合打印精度，一项约 5.18e-11 Å 初始坐标精度检查仍保留失败记录。
完整原生偏置逃逸随后通过：job1373062，316次MACE回调，E/F核验通过，候选升能被拒绝。
发现原生force使用最大分量：.04702的停点实际最大原子力范数.06619，不能等同ASE fmax=.05。
参考笼已完成job1373081：25次模型调用，E=-514.5727735075726 eV，最大原子力范数.00038866；
三种构图距离均保持Ih连接图，60个凸包顶点。原参考harness失败job1373064保留，非科学结果。
两组随机碳云开发对照已提交：Python job1373157（4条，已完成）、native job1373134（2条）。
Python通用方向两条均用完6000请求；恢复方向在2092/2072请求时均触发c1的12Å适用域门槛，
这是恢复实现不完整，不能归因MACE或优化器，也不能把该路径作为通用C60候选。
默认受限门槛保留；随后以显式per_atom选项实现P(Mr)，真实回归及边界见上方阶段结论。
本批为开发诊断，不是正式独立成功率验证。native仍有重连/压缩等不同策略，不能单因素归因。
这项用户指定验收优先于继续扩展旧水体系的历史容量实验。
[用户决定、接口证据与待完成项](2026-09-17-c60-shared-mace-acceptance.md)。

## 已完成补充：原生局部优化器的同起点对照

用户要求澄清Safe-total和原版内部优化器差异。已确认固定胞BFGSDRIVER→LBFGS→MCSRCH→MCSTEP；
此前五起点结果不包含原生核心。job1371850已完成相同五起点：原生4/5合格、1964搜索+5复核；
Safe500为5/5、1731搜索。不是完整驱动或通用排名。原生仅6次MCSTEP调用，不优先为插值新加后端。
保留Safe-total及曲率筛选、长历史候选，下一步回到独立输入/种子的完整SSW控制对照。累计67607搜索。
[算法差异、值得吸收的设计及证据边界](2026-09-17-safe-total-versus-native-lbfgs.md)。

## 新增的重要设计边界

原指令复核发现：随机分量种子被量化；12 Å邻域mask分支在第2/3原子超距时实际
清最后原子的分量；forbidden独立检查另有坐标chart依赖。因此行为恢复不能直接
等同于值得继承的科学算法设计。主线仍吸收局部模式/位移更新/CBD，保留原版
特有行为作显式对照，不静默改默认。没有新搜索效率结论。
[完整证据、实现进展与集成边界](2026-09-17-native-direction-reproduction-boundary.md)。

## 当前正在完成的里程碑：局部方向生成器组合与接入

已新增独立Python的局部系数选择和两组沿轴移动；当前不改变默认搜索。
已闭合c1+c6完整生成器6例、c4普通pair初始/位移更新4例、forbidden导致零方向释放1例，
均实际执行原生几何函数；另有完整fastbond表初始化/双向lookup核验。
c4两组路由已补齐完整动态组构造：分离组和连通回退2例独立复核通过，最大误差1.11e-16。
旧报告把vtable+c8误读成centralize，已纠正为group_atoms，组mask传递不再是未知。
已合入同一局部生成器，并以显式`recovered_direction`接入共享ASE驱动；
包括逐Gaussian位移、CBD刚体投影、真实淬火、MC前选轴/组选取及失败归档。
55项针对性检查通过；Cu13/Cu55/Au13×两规则共6条EMT接口轨迹返回17个含初态的
力合格结构，1573次搜索请求加17次独立复核，没有据此比较效率或证明不同盆地。
原始输入、源码快照、完整请求与结果在
`research/ga_ssw/evidence/recovered-direction-shared-smoke-20260917/`。
同一入口的MACE-OMAT-0-small检查已完成：job1369266，1 V100，45秒，退出0；
6/6臂完成，1534次搜索+18次独立复核，全部18帧达到fmax<=0.03 eV/Å，
最大0.02977091。每臂两次外步/每步最多4Gaussian，仅为接口与数值资格证据，
不能从较低请求数推断效率优势，两套旋转停止规则不同。
证据在`research/ga_ssw/evidence/recovered-direction-mace-e2e-v2-20260917/`；
初版prepared包未提交，已标superseded。下一步是上传水15和Cu55的LS衔接与更长固定预算。
原job1369369在第4臂期间因运行目录被并行任务错误改名而行政中断，非算法失败。
原始数据保留于`research/ga_ssw/evidence/recovered-ls-materials-20260917-preinit-replay-superseded/`。
已完成3臂；第4臂只有4342次请求轨迹、无最终结果，不重跑。父运行合计15793次付费请求。
Cu-LS零邻居是已有Cu回退表适用域限制；跳过相同初态的另一个Cu-LS种子，不改键长。
续跑job1369567只执行7个未开始臂，1 V100，最多42000次搜索请求；连同父运行仍低于原72000上限。
新冻结目录`research/ga_ssw/evidence/recovered-ls-materials-continuation-20260917/`含父运行事故、排除项及预算记录。
旧原路径下被重建的prepared目录未提交，不能作为原运行产物。
续跑1369567已正常完成：7/7臂，41470搜索+70独立复核请求，63项冻结源码检查通过。
水15的恢复方向两种子分别降低2.734/3.243 eV；各方法旋转标准不同，不可归因或排名。
LS两种子均有30个非零初始键、真实响应更新与表变化；seed11无新合格落点，seed29有10个（未去重）。
seed11八次失败均为真实淬火300步maxiter，并非LS预淬火或line-search失败。
job1369667已正常完成，重放两种子前两个LS淬火起点及Cu对照，比较Safe-total/history10、500、ASE、SciPy。
各600请求/600步/fmax=.03；总上限12000，加已用57263仍在原72000预算内。
20/20臂完成，8380搜索+12复核；Safe10/500、ASE、SciPy分别4/5、5/5、2/5、1/5合格。
相同起点的水轨迹会放大微小浮点差，不是旧轨迹严格续算；不据五起点改默认。
本系列累计65643次搜索，低于原72000上限。
[关键结论、反证和下一优先级](2026-09-17-recovered-ls-integration-results.md)。
证据`research/ga_ssw/evidence/recovered-true-quench-replay-20260917/`；不重写原SSW失败。
目标是非零LS初始化、真实响应更新与合格落点归档，不将三臂请求数作方法排名。
这些仍是实现检查，不是新增通用搜索效果证据；Q/compression、周期native模式、
该实验入口的checkpoint尚未完成，启动选轴也不能称完整native轨迹复现。
详情与已替代旧缺口见[集成边界](2026-09-17-run5-direction-integration-gap.md)。

## 当前阶段结论：旋转预算退出契约已完成有界材料验收

**最新推进：方向生成所需的独立 Python 几何与随机组件已补齐一部分。**
自动选组22项原指令对照已完成；新增选轴刷新在24个规范坐标案例和24个边界案例中
匹配原版的pair、抽样次数、接受分支和三类拒绝计数。Python随机组件匹配46个
已归档原指令输出，最大绝对误差1.39e-17。36项针对性测试通过。
这些是实现核验，没有新增材料搜索效果结论，仍不能称完整原版方向控制器。

**修正旧证据解释：**此前centered输入得到的6/9拒绝耗尽统计不能解释正常原版
搜索行为：check_forbiden混用wrapped邻居和原始端点，独立调用有坐标chart依赖。
已保留旧产物并补充canonical对照；完整选组→刷新→组方向→投影/混合的规范坐标
9项对照均通过，最大误差1.67e-16。该固定fixture也不是实际搜索频率估计。
原版VMB2种子被取整，存在十个主要种子区间（浮点上边缘可产生第十一个），
不能把此原始分量当连续各向同性采样；也不能据此断言完整SSW只有十种方向。
当前默认不变，下一步完成c4/c6生成分支、状态接入及跨体系效果验证。
详见[选择恢复与证据修正](2026-09-17-native-axis-group-selection.md)。

用户同意将方向收敛与有限预算结束分开。已实现显式`rotation_exit_policy='force_or_budget'`，
默认严格模式保持；保留残差、未收敛标记和放行原因。Ritz子空间耗尽及数值错误不放行。
91项针对性测试在计算节点通过。作业1366235完成12/12实验臂与1臂严格回归，
78000搜索+82独立复核请求；所有保存帧通过真实力/固定胞检查。
99次预算放行关联7个完成逃逸的合格落点，其中较紧/较松结构匹配下5/4个不同于初态。
它证明未达旋转残差阈值的方向仍可能有效，不证明普遍效率优势；零放行臂与严格复跑
也出现数值敏感的轨迹差异。保留显式选项，默认不变，不继续围绕本组数据调参数。
[完整结果与局限](2026-09-17-rotation-budget-exit-results.md)；
[设计与固定协议](2026-09-17-rotation-budget-exit-protocol.md)。
下一优先级回到完整方向生成/阶段更新的原版恢复与共同ASE驱动接入，随后LS验收；
GA/VC/RC不抢占主线。下面较早阶段记录用于追溯，不代表仍需重复讨论或执行。

## 2026-09-17 最新完成的验收与核心决策

AlOH26/brookite48、两个seed、三种方向求解器共12条完整SSW预算轨迹已完成；
72000搜索E/F、68帧独立力复核，外加1137次冻结方向/失败现场诊断。两个V100
作业1362345/1362485均退出0。所有68帧通过力/固定胞检查，并完成周期结构去重与
局部几何筛查；这不是DFT或正定Hessian验证。Broyden改善AlOH候选收益，但没有
在brookite全面胜过Ritz；默认不变。

**核心新结论：有限间距残差的收敛排名不等于中心Hessian模式精度排名。**
在保存的失败现场，Ritz单边残差略高于0.02而中心差分约0.011–0.014；Broyden
单边达标但中心差分约0.026–0.029。当前驱动强制残差达标，会抛弃一些已投入
Gaussian爬坡成本的近似方向。用户随后批准有限预算软化选项，控制实验已完成，
见上方最新结果；不再重复请求这一契约的批准，不直接放宽tol或扩展GA。

[完整阶段结论与产物](2026-09-17-matched-direction-results.md)；
[预注册协议](2026-09-17-matched-direction-protocol.md)；
[有限间距球面目标推导](2026-09-17-finite-separation-direction-objective.md)。

## 当前能用什么

见[固定胞使用说明](FIXED_CELL_USER_GUIDE.md)：公开入口、参数层级、约束支持及
失败语义。普通SSW、论文LS、原版启发LS共用Python搜索；FixAtoms/Hookean/slab
走专门入口。原版预算计数、CBD历史及部分周期语义尚未完全对齐。
Safe-total为当前研究配置，保留ASE/SciPy基线；不同时启用多种实验组件。

## 已经得到的决定

- AlOH三优化器对照：Safe调用较少，但低能发现没有一致优势，保留成熟基线。
  见[完整结果](2026-09-12-aloh-independent-budget-results.md)。
- OMAT十二臂SSW/LS实验：水15允许正常LS迭代上限退出能解除早停；但搜索
  优势尚不一致，保持显式实验选项，默认force不变。
- C4H6异常低能由直接ASE/MACE重现；不作为算法优势，也不继续围绕其调参。
  所有1334观察已做离线几何诊断，23次直接模型请求定位问题。
- 未触发新策略的C4H6配对差异从初始求值的微小数值差异开始，不能归因于
  退出策略。无需为追求逐位一致重跑长轨迹。

证据与边界：[OMAT有效性诊断](2026-09-14-omat-validity-diagnosis.md)。
几何连通、小力和归档数量都不等于物理稳定或不同盆地。

## 2026-09-16 重排：先完成算法验收，再评价改进

用户的首要交付是完整、独立、可接ASE的SSW系列实现；超过BasinHopping是
效果研究问题，不能偷偷变成复现完成的必要条件。也不能反过来把能运行当作
算法已复现。两条验收线分开：

- 算法验收：每个核心阶段明确原理/原版证据、当前行为、必要缺口，以及有意
  保留的独立替代；不存在未声明且改变搜索行为的遗漏。无需复刻已知原版缺陷，
  也不要求不同GPU上逐位相同轨迹。
- 效果验收：真实体系中产生物理合理且结构不同的落点；改进设计的保留价值
  另用同总成本对照评价。没有优势就撤回优势主张，而不是无限延迟基础版本交付。

当前最重要的下一项工作是关闭固定胞SSW的方向生命周期缺口：对照已有原版
caller证据，明确一轮逃逸内随机参考方向、逐Gaussian重定向和Broyden历史
分别何时创建、更新、清空。当前Python在每Gaussian调用独立求解器，Broyden
模块明确使用独立停止规则；因此已有Broyden函数不等于原版CBD完整复现。
只恢复这些会改变proposal的状态转移，不以补全全部LASP代码为目标。

第二项是LS材料参数初始化语义。区分“原版确实使用的经验fallback”与
“本实现猜测的参数”：前者可作为复现模式明确实现，即使其材料表现不佳；
不能因为没有普适物理最优表就认定无法复现，也不能把raw lookup直接当完整
初始化。优先核对Ti/O一个具体调用链的缩放、筛选及覆盖，不自行拟合Al/Ti表。
已有HCO正常预算出口的实际机制证据继续保留，默认策略不变。

达到以上有界验收后，将生产源码/必要测试/使用入口整理成可独立评审的版本；
不把全部研究产物混合提交。再执行下方材料SSW/BH对照，随后GA增量，最后VC/RC。
不等待固定胞在每个体系上胜过BH才推进其余必要复现功能。

本轮重新核对代码、历史依据和18臂终态摘要：AlOH为4完成+2预算截断；HCO为
8完成+2预淬火失败+2预算截断。未新增算法实现或计算，不把再次汇总当新效果证据。

## 2026-09-17 方向生命周期核查

论文参考路径已有明确契约与回归：同一 escape 重用初始随机 anchor，方向求值
不含累计 Gaussian，偏置淬火保留全部 Gaussian；相关测试 15 passed。
没有改变默认算法，也没有新增材料搜索。

发现并纠正三份旧原版报告的同源错误：post-update 条件应为
`status != Allopt` 才重入 CBD，原先误记为等于。直接执行原始比较/分支片段
4/4 通过。它关闭了一个错误解释，不等于原版整个方向生命周期已经复现。
下一步沿纠正后的分支核对阶段切换与历史 reset；随后执行 LS 初始化审计。
证据、命令、局限及纠错记录见[本轮验收](2026-09-17-direction-lifecycle.md)。

## 2026-09-17 新增结果与已批准的核心设计

已执行原版 update_mode0 指令片段：在核验分支中，旧 n0 被当前位置相对选中
轨迹记录的归一化位移覆盖，再传入 gen_randommode；4/4 通过。还不能把这一步
的输出称作最终方向，但已证明原版阶段输入不能简单等同于最初随机 anchor。
新的 CBD stage 逻辑重置有效 Broyden 历史的静态链也已核清；不要新增跨阶段
history carry 来修复一个并不存在的生命周期遗漏。

用户已同意[方向状态设计边界](2026-09-17-fixed-direction-state-decision.md)：
同一 ASE 驱动下保留论文规则与单独的原版恢复规则，避免静默覆盖旧入口语义。
实现前先做[两种参考方向的理论分析](2026-09-17-fixed-versus-transported-anchor.md)。
下一项恢复 selected_record、模式生成、预旋转到实际 bias anchor 的完整链；
不能把已证实的位移输入直接视为最终 anchor。默认算法未更改。

LS Ti/O 无 custom 初始化链已闭合到原有矩阵构建边界：root 独立重复原指令与
Python 对照，成键数2、矩阵误差0。已加入显式选择的 TIO 参数表，默认不变；
[审计与产物](2026-09-17-ls-initializer-audit.md)。相关 LS 测试11通过，方向测试38通过。
尚不证明材料搜索增益；首次完整软化 E/F、周期与受约束计数等边界保持明确。

## 下一组材料输入已筛选（尚未提交）

### 2026-09-17 方向恢复的新结论

**最新阶段：CBD阶段已组合成独立Python/ASE实验求解器，尚未进入默认搜索。**
预旋转、正曲率有偏重入、负曲率无偏转换、history reset与端点力复用已接合；
原始力代数12例、调用者8例及保存力6例通过。新增生命周期测试4通过。
V100作业1361832在AlOH26/brookite48上完成有界OMAT-small接口检查，分别8/6次E/F；
不是完整SSW搜索。关键发现是原版旋转力与HVP残差判据相差`10*dr`尺度，
同名同数值tol不能用于公平求解器比较。详见
[阶段实现、真实输入检查与精度换算](2026-09-17-cbd-stage-and-tolerance.md)。
后续数值比较必须统一残差精度或比较同预算曲线；完整方向生成、约束和共享驱动
接入仍未完成，下面原有几何组件证据保留，不代表新求解器已生产验证。

**最新进展：局部原子组几何已移植为纯Python函数，但完整方向模式尚未接入。**
`native_local_group`复现指定轴和指定原子组的无穷小转动；并非RC-SSW。
root已在乙烷/甲醇/苯的6组几何中执行真实原版局部生成、刚体清理和混合，
与Python函数+刚体投影比对，最大误差1.11e-15；针对性测试5通过。
实际生成与投影已不再用注入向量/恒等替代，但轴、组成员与warm-cache仍显式给定。
[完整证据与代码边界](2026-09-17-native-local-group.md)。
下一项最高优先级是恢复轴/组的自动选择及阶段更新，再连入共享驱动；
真实材料端到端搜索尚未运行，不宣称性能改进。

已把“阶段位移输入”进一步闭合到局部原子组分支的原始混合运算。
位移记录及0.001总范数边界14/14；预旋转结果保存到实际anchor的复制6/6；
c9缩放前缀1组误差0；c6混合14/14，最大误差1.11e-16。
后三者不包含真实PES求解；c6混合明确注入局部向量并用恒等替代刚体投影。

关键算法解释：保留项为1.2倍局部权重总和。在非零单位位移、非负权重、
单位/零局部分量且投影保持位移的条件下，混合不会反向，偏转上界56.44°。
它不是额外新增的角度阈值，更不是后续CBD/Gaussian方向的保证。
零位移仍可由非零局部分量生成方向，不能直接当成Allopt条件。
[推导、执行证据和局限](2026-09-17-direction-mixture-cone.md)。

当前仍未接入完整原版方向模式，新增了独立Python几何函数，默认驱动未改。剩余主线是局部模式的
选择、投影和预旋转接入已批准的共享驱动，然后做同预算跨体系对照；
不因这一几何性质声称搜索更好。Lmode_Q对应的可选c5数据契约尚未恢复，
不能把它静默省略后称作原版默认。这个缺口暂不需要用户提供资源或作决定。

- 主搜索案例：上传AlOH26，沿用既有两帧输入。已有OMAT E/F/stress资格与
  多步SSW证据；用来比较可靠低能结构发现和实际成本。
- 刚性对照：已有brookite48，输入
  `research/ga_ssw/prospective/complex-vc-feasibility/inputs/brookite48.extxyz`。
  既有固定胞初态/落点fresh与后续精修Hessian证据可追溯至
  `research/ga_ssw/evidence/complex-vc-gate-qualification/`。保持固定cell；
  该接近稳定晶体的案例用于返回原盆地/无改善时成本，不作为必须找到更低能
  的成功率任务。不同cell多晶型不能直接充当固定胞可达目标。
- 12原子TiO2 SI结构保留为低成本接口检查，不代替复杂搜索证据。
- Ti/O 无 custom 初始化已取得原指令证据和显式表；Al 与其他材料仍未核验。
  下一轮可先完成SSW/BH比较，LS表核查为有界独立任务，不调Cu fallback补邻居。

此阶段已核对输入、文档代码语法和公开API，未新增PES或GPU任务。正式比较还需
把现有BH runner的固定EMT/GFN2输入接线改为同一材料/OMAT设置，并核对统一计费。
这是下一步实际实现工作，不能把当前案例选择称为已经执行的公平比较。

## 实验与发布边界

当前没有因模型异常自动重试或追加长搜索；不更换用户指定模型，不默认添加
排斥势。新实验应先固定预期信号与预算，结果不支持假设时不靠调阈值挽救。
当前研究分支含大量未提交实现；阶段文档不是已合并或发布的稳定版本。
历史过程与旧状态移至[历史记录](2026-09-14-mainline-history.md)，不得作为当前队列。
