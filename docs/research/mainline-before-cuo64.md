# 当前主线：独立 ASE SSW 家族与可验证的收益

更新于2026-09-11。本文是当前执行计划；历史阶段决定存于
`2026-09-10-mainline-before-evidence-consolidation.md`，不再作为待执行任务。
实现清单与结果边界见 `ssw-family-current-status.md`。

## 目标与设计取舍

完整实现可接入 ASE Calculator 的独立 Python SSW/VC/LS/RC/GA 本体。
SSW 与 VC 是公共核心，LS/RC/GA 的已知真实缺口可并行补齐。正式搜索不调用
LASP/JAR；原程序仅作为静态与隔离数值实验的行为证据。沿用 PAM Safe-total
作为工作数值后端，任何更换都需要可比成本下的证据。

数学/物理一致性优先于照抄原版：Gaussian 能量与力必须配对，偏置在一次
局部优化内冻结，变胞优化 E+pV 并使用同一坐标的精确力/应力链式法则。
原版旧 Gaussian 力重复累加、辅助 LJ 角导数和非旋转不变 Broyden 内积等
已证实问题不作为生产默认。独立修正、数值替代和原指令等价分别标注。

当前不推荐普遍开启 LS，也不建立新的混合策略调度器。C4H6 与高度策略
对照已经表明成本、完整分子覆盖和不同极小值覆盖可能互相冲突。有限实验
不能证明不存在任何未来收益；每条研究线以明确、可证伪的问题推进。

## 当前执行任务

2026-09-11 XXXII 后端修正核验：新的研究 ASE adapter 对外保持 172 原子、
允许联合变胞，内部使用显式固定超胞并折回 E/F/stress。33 次真实 EFS 已完成，
1×1×2、1×1×3、2×2×2 在初态和旧故障点一致（最大能量差 2.2e-11 eV）。
旧跳变消失，检验的 RC/应变方向导数最大误差 1.6e-6，仍保留已知 erfc
力能不完全一致的边界。证据：`xxxii-replicated-qualification`，内部共
14964 atom-evaluations；API 计数不能直接与原胞开销等同。
从原始输入重跑 RC-VC 已完成：central Ritz 用3559API完成12段爬升和真势
变胞淬火；同后端forward dimer用1319API在第5段旋转失败。候选高0.31574eV，
MC拒绝。独立严格淬火再用542API，能量差仍高0.31314eV；不称低能发现。
公共VC/RCVC已可显式传入central Ritz及独立E/g调用预算，默认dimer不变。
真实完整轨迹的3557次搜索请求经公共接口精确离线重放，0新增PES。
二阶有限晶胞核验已完成：两端点、两步长各519个非平移本征值均为正；候选最软曲率约2.87e-4 eV/Å²，仍仅支持所用GAFF与有限晶胞内的近似局部稳定。
严格端点的周期结构匹配在三组显式容差下均不匹配；180条键长最大变化0.00767 Å。不能从单个带标签二面角的近180°变化排除等价原子置换。
Cu13两项回归差异已定位为环境依赖，旧冻结代码同样复现，未放宽容差。
TiO2 phase139同预算central Ritz对照已完成：与dimer同样完成8段、第9段1500EFS预算用尽、无landing。没有跨体系收益，保留实验选项且不改默认。
VC已补分阶段实际调用与淬火残差记录；Cu/EMT两组完整轨迹逐调用精确不变。两个TiO2冻结失败目标另2EFS核验：偏置梯度范数0.3774/0.3532，原子部分主导，并非应力容差单独导致；该系列累计11392EFS。停止此单例参数扫描。
原生LJ配对导数、应力累加和绝对晶胞体积归一化已由6组原指令核验闭合（零PES），见`native-lj-stress-producer.md`。普通CSSW直接接收应力，NPT的额外负体积转换不能混入VCSSW。`cal_pes`还包含日期后处理，但静态核算本轮2026-09-09/11日期差项为零，不能据此否定本轮数据。globalcompress C7/C8/C9分段公式与调用端归一化已核清，57个原指令边界/掩码案例完全相同；不因公式复现而启用经验方向。
当前核心补齐：公共constrained SSW可分别指定物理固定原子与方向排除原子，原TYPE4两种掩码由3组原指令核验。Cu28/EMT完整默认轨迹精确不变，546E/F验证接口，347项回归通过。514原子TiO2@Au24O4 MACE两组完整搜索已在V100完成，共536E/F，各自降能约2.35eV且满足原输入力阈值，不能称不同稳定basin或双掩码收益。独立核验8283EF已完成：两个落点两步长Hessian均正，但共同初态有负曲率。额外612EF无偏置对照只解释原降能0.037eV，严格共同初态的两步长Hessian现已均正，重跑两组仍降能2.49675/2.49690eV（523/413EF），累计13009EF。新baseline落点两步长Hessian已正。独立seed7/101四组共12次尝试完成：9个力收敛落点，3个预算截断，10167EF；方向掩码两seed得到略低best，但样本与预算终止不支持通用排名。两组best已各通过两步长完整Hessian，最小曲率0.02426/0.01576，最终分别低4.65719/4.77842eV；TYPE4累计30997EF，此线停止加预算与调参；详见`type4-source-mask-results.md`。
下一项VC核心验证转入可追溯AFLOW CuO：8原子原始晶胞及其64原子等价周期扩胞的MACE表示检查、严格变胞淬火和195维两步长Hessian均已通过，共806EFS。新增压力为零的PQC/joint匹配对照正在准备，未宣称此模型等于真实CuO磁性/相图。
详见[XXXII完整结论](xxxii-replicated-rc-vc-conclusion.md)。图距离 ≤3 的
拓扑对越过展开盒的分量半盒条件时在 PES 前失败，不动态改变复制数。
原 LASP custom neighbor 的 special-bond 位编码与实际选择仍须单独核验，
不能把 stock LAMMPS 的观测直接认定为原版 LASP 缺陷。

## 此前证据背景（以下历史“下一步”不作为当前待执行任务）

2026-09-11追加：按“真实失败→优先级→原理/反编译→最小实现→真实对照”循环推进。
团簇重连接已恢复为纯NumPy/ASE组件，54个隔离原指令几何对照通过，并以
`reconnect_distance=None` 接入普通SSW/LS，默认关闭。完整C60对照完成1,462 EF；
前1,381次请求与原LS逐项坐标/能量/力完全一致，只在真势landing入口恢复
几何。最终单连通但仍高4.95161 eV、存在2/4配位，MC拒绝；未获得完整笼发现。
因此保留实验选项，不默认启用，也不继续围绕该单例调重连接距离。
普通SSW的179EF落点经另23EF严格淬火支持同basin。仅改变原版来源
0.1eV早退裕量的对照已完成1315EF，也得到C58+C2、MC拒绝；因此不能将
碎裂单独归因LS，或把延长爬坡当成收益。下一项核原版局部/全局direction
混合与归一化；不改变生产默认。详见[c60匹配对照](c60-matched-controls-conclusion.md)。
原版localpair几何已独立实现，7组含C60的原指令对照误差<=1.11e-16，默认不接walker。单独归一化/邻居协同两控制共308EF，加21EF严格复核均支持同basin；同源0.1eV交叉对照已完成：unit-local1984EF、协同1744EF，均为高能开放笼且MC拒绝；该系列累计8560EF。停止此单例参数扫描，继续核原版globalcompress几何。

复杂48原子TiO2 phase-87/139已通过26EFS应力预检。phase-87 PQC与joint原始
各1500EFS均censored，保留原结果。PQC从completed边界追加448EFS后完成
真势cellquench及reset检查：+5.10869eV、MC拒绝，累计1948EFS；joint从
真正completed边界追加568EFS后，在Gaussian index8的biased quench达到300迭代
上限而未收敛，没有landing；错误续跑的500EFS单独保留且不用于对比。
冻结九项偏置目标的同起点对照已完成：memory10用305EFS仍未收敛，memory400用218EFS收敛；含高度核验共524EFS。起点/失败点另26EFS方向导数检查支持局部梯度一致性。同1500EFS预算的joint memory400原始输入单步完成Gaussian0–8后censored；从完整边界另280EFS完成最后Gaussian/真势cellquench及独立fresh，累计1780EFS。landing高2.61362eV、MC拒绝，支持数值流程完成，不支持低能发现优势；生产默认仍10。
phase-139同1500EFS的memory10/400均完成8个Gaussian后预算终止，无landing；不支持跨初态收益。TiO2该诊断系列累计9890EFS，包含失败与无效续跑成本。
TYPE4负载簇514原子的固定1..297约束已核清，初始constrained quench与fresh
检查已完成（含预检共35EF）；尚未开展该514原子完整SSW搜索。
XXXII172原子新增固定G三步长检查52EFS：胞主残差与erfc近似导数失配一致，RC仍有约1.7e-7未归因残差，不称严格保守。保留原优化容差准备实验性RC-VC整步，独立检查落点；导数检查累计196EFS。XXXII RC-VC整步memory10用385API在第一偏置淬火失败；同冻结目标400用291API收敛，但全程400用741API仍在第二偏置淬火失败，均无landing。归一化坐标/300iter预算与历史作用待区分，不称400解决完整RC-VC。
XXXII后续完整centralRitz测试确认约0.235eV后端能量不连续，已定位skewcell的1–4特殊邻居半胞判据；等价2/3/8倍复制结果相互一致。暂停对该原胞轨迹调优化器，先验证复制表示与原版molcry_only邻居语义。详见[周期排除不连续](xxxii-periodic-exclusion-discontinuity.md)。
完整缺口清单见 [核心功能审计](2026-09-11-core-gap-audit.md)。

2026-09-11最新结果与优先级见
[整体进展](2026-09-11-overall-progress.md)：用户指定MACE-OMAT后，完整C60
LS单步完成12个偏置阶段、真实淬火和fresh检查，1,395次E/F/556.698秒；
加4次预检共1,399。数值landing收敛，但为C58+C2、间距16.2625Å、相对初态
高8.68519eV，MC拒绝。因此不能把更换后端当作解决完整笼搜索；下一步优先
核清原版压缩/蒸发处理，再做同MACE后端的普通SSW/LS对照。该CPU实验已结束，
未申请V100或提交GPU作业。以下为此前各线的证据背景。

1. 困难 C60 memory400 完整LS单步已完成：1017次E/F、501.804秒，
   九个偏置淬火收敛，但第十次软模旋转残差0.03911687>0.02，99HVP+1中心
   力请求后停止。旧memory10失败于偏置淬火，两者均无landing；不能把更早
   以不同原因终止的调用差称为搜索效率提升。精确缓存重放和独立精度对照
   已完成：同一冻结点、相同dimer，fresh GFN2 accuracy0.001用100次请求
   仍未收敛，accuracy0.00001在28次请求收敛，残差分别0.0297393/0.0151592。
   三精度下固定dimer/Ritz方向均通过0.02，不能据此前9对100次的标签更换
   solver。完整单步的单因素精度对照现已完成：1,030次E/F、738.919秒，
   完成10个偏置淬火后，在Gaussian index10遇到SCF250迭代不收敛；仍无
   landing。完整缓存重放已恢复失败请求：全部1,029条成功请求坐标误差0，
   失败call1029位于旋转完成后的displaced background-force评估，并非dimer内部。
   两精度的最后偏置入口均为C58+C2，片段间距分别3.773/6.925Å；尚无真势落点。
   当前优先核清论文已有globalcompress/vapor_cri/Ratio_Local的实际动作，
   不盲目增加SCF上限，也不凭碎裂现象直接添加连接约束。
   优先保证力精度、差分步长与软模容差一致，不能把冻结旋转成功当作LS搜索成功。
   全standalone consumer的memory接口遗漏已补齐，含GA直接quench与固定
   基底；默认仍10，不新增自适应策略。
2. 八组 Al8O14H4 / brookite48 搜索及独立资格已完成，分别花费
   15,467 和 5,708 次 E/F/stress。16 个源端点 fresh 检查通过；12 个按域
   去重的严格淬火点均有两步长正有限胞 Hessian。joint Al8O14H4 候选在
   同域严格淬火后低 0.2005702 eV，仅支持该模型和有限胞局域稳定性。
   后续性能实验需另定预算和种子，不能把单种子结果称为搜索效率优势。
3. 原版 XXXII 势函数已按反编译公式转为 stock LAMMPS 并接入 ASE，
   172 原子真实接口测试暴露 Coulomb 截断和近似 erfc 的数值误差。
   零PES分析已定量解释原子方向erfc残差，cell/RC残差仍未闭合；不盲目追加精度试跑，
   不在能量/力契约尚不清楚时开展完整搜索。
4. 原版核心逆向只追踪可影响正确性或可检验收益的问题。curv_real 已确认
   为旋转前、含 LS 回调的力差投影；原版预淬火出口坐标/力对应关系、
   stress 写入消费和 CBD 历史后半段仍有证据边界。各 walker 已补齐失败时
   最后接受状态和冻结 objective 的保存，避免用扩大拆码覆盖率替代收益。

## 已有依据与保留决定

| 问题 | 当前证据 | 决定 |
|---|---|---|
| 工作局部优化器 | 同 31 个冻结 Cu13 偏置问题，Safe-total 31/31、隔离原指令 LBFGS 29/31；共同成功集原版略省调用 | 保留 Safe-total，不宣称普遍优胜，也不等同完整 BFGSDRIVER 比较 |
| 原版 87° 高度 | 两体系 132 个 stage 没触发增长；效应来自初始化高度。Cu13 出现一个额外正内部 Hessian 结构组，但总成本更高，能量高于初态 | 保留明确可选 profile；不默认替换，不把更大高度归功于动态角度调节 |
| 解析最小角度高度 | 无迭代增长参数；当前 Cu13 成本更低但覆盖更少，C4H6 无低于初态新结构 | 保留实验性简化策略，不能声称 dominates |
| 胞自由度 | 后验胞淬火、2014 启发的胞/原子分块、联合 log-strain 三个可执行不同算法；八组对照已完成 | 不再把 local cell-relax 冒称联合 VC，已通过所列有限胞资格；单种子不支持效率排名 |
| RC 的 krot | 已恢复有限域转动继承；树形可达空间可由现有 torsion 重参数化覆盖 | 不因原版存在参数就新增参数；优化条件数收益需单独证据 |
| LS | 生命周期、强度响应与周期 image-resolved 扩展已实现；已有分子结果无一致优势 | 困难 C60 检验具体失败机制，保持普通 SSW 基线 |
| GA | TYPE0–4 算子/控制器及初始化已实现；部分真实 EMT 集成结果，复杂分子晶体效果未证实 | 不以 Java 身份推定优于 ASE-GA/USPEX，不把辅助 LJ 调用隐藏为免费 |

## 后续推进与停止条件

每次新增计算先写明初态、模型适用域、参数来源、种子、总预算、失败定义和
保留/删除判据，再冻结源代码与输入。用户已授权常规有界 CPU 资源安排；
旧的长 campaign 不因文档更新自动启动。当前有界 GPU 作业状态以上方当前执行任务及对应证据目录为准。

若闭合原版语义显示它只是相同可达空间的重参数化或与一致目标冲突，就结束
该条忠实移植路线并保留证据。若真实对照没有收益，不靠增加奖励/阈值或反复
使用同一测试集调参挽救。若失败集中于确定核心原因，先隔离该原因再最小修正。
多个预算受限样本仍不足以确定有效性时，明确“证据不足”，不伪造饱和结论。

全部单元测试只说明公式/接口；小力不是稳定相，有限胞正 Hessian 不是完整
声子稳定性，结构描述符不是动力学连通性。论文/模型/源码无法访问时保留
具体缺口；目前可执行任务不因这些局部缺口停止。
