# Existing full-direction bundle on sourced C60 defect

Question: does the already implemented pair/group/local/global/displacement-memory direction bundle alter useful local-defect exploration relative to global+same CBD? The prior global+CBD comparison has40 force-qualified landings but0 Ih; early selected quenches return to the defect or non-cages. This leaves a genuine untested direction mechanism at this input. Prior randomC60 comparisons showed no consistent advantage; no benefit assumption.

New4 arms: identical source and seeds1101/1102, with/without nativeLS,10 attempts and12000 search requests each,11 fresh checks each. SingleV10060min cap, whole program59min. Existing parent runs are controls, not rerun. Match CBD5/15,ftol.2/.02,max40 and all SSW/LS/MC settings. Use existing selector50/.5/.5 from run_c60_random_python_development.py; these are recovered/experimental settings, not tuned values. Initialization is Python input-to-initial-quench displacement; near-zero initial displacement can be tie-dominated. Not exact native startup parity.

Only configuration changes; no core algorithm modification, new parameters or scan. Full recovered_direction replaces separate recovered_rotation, because API makes them mutually exclusive. Record both effective settings and persistent state. Coupled pair/group/anchor/state evolution means results cannot isolate a single selector or memory component.

Qualification: initial and every recorded landing independently fresh checked; graph against Ih and original defect at1.64/1.7/1.8Å, energy relative to reference, failures and costs retained. Compare common search prefixes and best including initial structure. A graph change or MC acceptance is not success; no Hessian qualification of every landing is promised.

If no useful cage rearrangement appears, end this local-defect branch rather than increase its budget or tune selector/LS parameters. If useful cage/Ih appears, preserve as bounded mechanism evidence and assess transfer to existing other-system data before any default decision. No automatic successor, retry/resume, budget extension or production acceptance claim. Parent SSW/LS study and these arms still share one structural input; two seeds are not independent structural validation.

Execution: CPU1484967 passed actual settings/input/curvature preflight (zero PES). GPU1484974 submitted with explicit group account,8V100V0/rush-1o2gpu,1V100/60min, frozen3f35a98. Runtime preflight checks the frozen shared runner/plan before evaluating the model. Source and saved-stage milestones pushed through3f35a98.

Readout GPU1484974 completed20m12, CPU1484988 exit0. Follow-up geometric identity CPU1485232 failed because directory glob included preflight-used.json; fixed iteration to actual arm checks paths. Failed log preserved, no PES involved.
