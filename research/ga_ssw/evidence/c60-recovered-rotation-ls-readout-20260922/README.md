# LS + 恢复 CBD 的独立读出

CPU1441616以afterany:1441611:1441612自动执行，零新PES。实际入口 `analyze.py`，输出 `analysis-1441616.json`，禁止覆盖。

复用已执行的heldout审计：初态请求只计一次、逐记录与总请求/账本闭合、失败/拒付保留、初态/最佳及条件首次笼的独立复核、参考C60图阳性检查、参考能量与笼资格分开。另核对LS响应eV/atom到meV/atom换算和失败预淬火不更新。新增LS计算成本在 `new_cost_only`，旧无LS对照只引用既有summary并单列，不重复PES。

主agent对初稿审查未通过：原稿误用随机输入作参考，且只列对账字段、没有强制校验和非零失败出口。原稿保留 `rejected-draft.py`，不执行；实际程序重用已验证审计并经静态复核/语法解析。此新适配器尚未运行，不把复用程序的旧测试视为本次端到端验证。最终退出0仍只代表审计完整性，不代表成笼或性能优势。


## 实际完成

CPU1441616完成，23秒，errors=[]。新增120000搜索/114527实际calculate，另4次独立E/F均合格；两条均预算截断，无目标笼/参考能量达标。相对复用无LS对照一好一差；见docs/research/2026-09-22-recovered-rotation-ls-results.md。不升级默认、不自动补跑。
