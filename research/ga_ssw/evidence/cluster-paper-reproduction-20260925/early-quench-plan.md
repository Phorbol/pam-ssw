# Saved LJ38 pre-fragmentation true quenches

Question: are intact early biased endpoints already in the catchment of a distinct force-qualified connected minimum, or do they only return to the starting basin? Stage replay established late fragmentation but cannot answer this question from biased geometry alone. Competing explanations (early useful transitions destroyed vs no useful transition) imply different next decisions. This is an outcome-selected diagnostic, not a new global search or an unbiased early-stop comparison.

Inputs are paper seed25092501/outer0 and seed25092502/outer2 from stage-probe-repaired-runs. For both use Gaussian indices0,3,6 (first/fourth/seventh stage) and the final intact endpoint before fragmentation (7 and8 respectively). Eight independent true quenches. Keep original Safe-total/history500, fmax0.01eV/Angstrom, steps1000, epsilon1eV, sigma2.7Angstrom. No bias, constraints, reconnect, coordinate perturbation, RNG or algorithm change. Each true quench is capped at1500 search requests; one separate fresh E/F per returned result, so12000+8 ceiling,1CPU5min Slurm/240s internal. No automatic retry or continuation. Original stage probe cost17800 (including output-failure replay) remains separate and charged.

For each endpoint preserve converged/failure/censored status, actual cost, input/output, fresh force/energy, connectivity at1.3 and1.5sigma, and proper-RMS/graph identity to that outer step's starting minimum. Comparing endpoint energy with the initial saved minimum does not itself determine identity. No TS or kinetic interpretation. Inputs differ between outer steps; do not present this as a shared-start paper-vs-global comparison.

If all return to the starting basin, close the premature-climb explanation for this subset and do not implement early quench. If some produce distinct intact minima while full paths fragment, late continuation discards potentially useful endpoints in this subset; retain a hypothesis for an independently controlled mechanism comparison, not an automatic default or a fixed-depth recommendation. Old C60 early-quench negatives remain contrary evidence against a universal early-stop rule.
