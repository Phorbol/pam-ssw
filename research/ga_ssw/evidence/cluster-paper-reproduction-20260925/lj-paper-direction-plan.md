# Matched SSW2013 initial-direction ablation — 2026-09-25

Parent: lj-pilot-plan.md (already running global-direction pilot1488221; no outcomes inspected when adding this arm). User asks to reproduce useful paper effects. Original SSW2013 §2 Eq1–2 uses normalized Maxwell direction plus lambda times an unnormalized pair-displacement direction, lambda uniform[0.1,1.5], pair separation>3Angstrom. The existing `sample_initial_direction(mode='paper')` implements this. No new kernel, numerical parameter, default or native whole-direction controller is introduced.

Only change versus parent: direction_sampling='paper'. Same potential, initial SeedSequence children, two seeds, N55/38, optimizer/CBD, adaptive height, widths, acceptance, force thresholds, per-arm and total bounds, prospective target validation and positive-control gate. Total extra ceiling2million search requests+8fresh, one CPU task, 90min Slurm/80min internal ceiling; no GPU. Separate output `paper-direction-runs`. Wrapper and base are both in Git; execution.json records the actual committed revision, effective settings, and base snapshot. Reproduce using `run_lj_paper_direction.py`, not running the base snapshot alone.

Expected discriminating signal: better target discovery at common request prefixes with paper pair mixing supports retaining that source design on this panel. Similar failure in both arms motivates inspection of initial qualification, CBD/climb/quench and total costs; it does not license escalating GA or tuning to one input. Random draws after direction generation differ; same seed is a paired input, not an identical stochastic trajectory. Two seeds are development evidence only. This is closer to the paper's direction recipe, still not exact publication success-rate reproduction because initial distribution and numerical realization differ.

The global arm remains unchanged and its failures remain in the denominator. If an arm stops before LJ38 because both its LJ55 controls failed, that missing pair remains not-run; do not compare unmatched LJ38 outcomes as a complete ablation.
