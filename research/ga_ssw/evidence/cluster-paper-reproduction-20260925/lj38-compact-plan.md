# LJ38 compact-observer continuation of the frozen development protocol

Question: with observer-induced repeated full-history copying removed, does the unchanged independent SSW reach its original request budget and/or discover the published LJ38 target? Existing four trajectories stopped at30minutes with275113–340393 requests, not at the800000-request limit. Competing explanations remain insufficient executed search versus poor proposal efficiency; an observer-only replay distinguishes recording limits from changed numerical behavior, but a negative two-seed result will not establish universal algorithm failure.

Two previous inputs/seeds25092501/02; both global and paper direction settings. Numerical settings, initialization, complete-step cap2000, per-seed request cap800000 and wall cap1800s remain unchanged. Use run_lj_pilot.run_one with compact_observer=True. Validate exact initial coordinates and effective settings against the corresponding previous run before PES calls. No restart from target, best structure or censored partial state: rerun from each identical original input/RNG. PreviousLJ55 positive-control gate is already satisfied (4/4energy/force/geometry), so do not rerun those cases.

Two CPU tasks, one per direction, two sequential seeds per task. Per-task search<=1.6million, fresh<=4, nominal internal wall<=60minutes, Slurm65minutes. Combined<=3.2millionsearch+8fresh,130 allocated CPU minutes; noGPU, no automatic retry or budget extension. Postprocess geometry without PES. First target energy/force candidate triggers normal safe-boundary pause; require independent geometric qualification. Force-converged non-target minima remain failures to discover the target, while budget exhaustion remains censoring.

Acceptance of engineering equivalence: all common completed boundaries must match previous status, acceptance, costs and energies (numeric tolerance1e-12eV); initial arrays exact. Preserve any discrepancy as a blocker to algorithm ranking. Report complete and partial paid costs separately. Compare target discovery and best energy at equal request prefixes; wall performance reflects both copying and execution and is not inferred from the copy-only benchmark. Report results as development on reused inputs, not independent validation or exact replication of published success-rate distributions. Do not adjust width, height, rotation, quench or MC parameters from intermediate outcomes.
