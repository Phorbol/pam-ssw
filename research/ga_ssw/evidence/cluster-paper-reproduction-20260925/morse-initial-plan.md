# Prospective random-input qualification, not a Morse search comparison

Before investing in short-range Morse search, test whether the currently specified diffuse initial ensemble reaches a connected starting cluster. Competing explanations for a small gradient: a cohesive stationary structure versus spatially separated fragments with negligible forces. Use the same volume-uniform5.5r0 sphere, child RNG construction and seeds25092501/25092502 as the LJ operational generator, at N29/80. This is not claimed to be the undisclosed original SSW ensemble. No resampling, container or parameter tuning.

Existing ASE MorsePotential: rho14,epsilon1,r02.7, cutoff100/101r0 (tails already underflow before switching); Safe-total/history500, fmax.01, max1000optimizer steps, at most1500requests/90s per input. Four inputs, <=6000search+4fresh, one CPU task <=10min, no search moves. Retain raw and final coordinates and all failures. Observe component sizes at1.3/1.5/2r0, not only optimizer convergence. A single pair at1.5r0 has force about0.00945epsilon/r0-unit-converted eV/Angstrom for these scales, illustrating why weakly interacting fragments can satisfy a0.01 force tolerance. Connectivity here is a distance diagnostic, not a Hessian certificate or general bonding definition.

If fragmented inputs pass force convergence, do not present that fact as algorithm convergence to a useful cluster and do not proceed to an unqualified performance claim. The next choice would be a separately declared compact ensemble or explicitly justified confinement/reconnection; neither is enabled by this qualification script.

## One prospective density correction after the diffuse qualification

Four of four diffuse starts converge by force but remain fragmented (48/94/745/754 search+4fresh total). This is a physical input-ensemble problem, not proof of SSW failure: no SSW steps have run. Retain the original inputs/results.

Test one size-dependent radius, not a radius sweep: R/r0=1+(3N/(4*pi*sqrt(2)))^(1/3), using fcc number density sqrt(2)/r0^3 plus one r0 shell. This is the container-size construction reported in BH1997, here used only as an explicitly independent random-initialization convention; no container forces/constraints are enabled, and it is not the undisclosed SSW2013 input generator. Scale the same unit-ball samples, preserve seeds and the exact optimizer/call/wall caps. Output `morse-compact-initials`, additional ceiling6000search+4fresh/10CPUmin. A fragmented/nonconverged outcome is retained, not repaired by per-seed tuning. This test selects an explicitly reported usable input protocol, not an algorithm winner.
