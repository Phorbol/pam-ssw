# LASP LJ comparison readiness

Read-only evidence review, 2026-09-25. No LASP run, calculator call, or job was started.

## Confirmed

- An archived LASP executable is present at `/home/gengjianrui/bin/pam-ssw-research/ga-ssw-20260909/GA-SSW_program/lasp`; the TYPE0-LJ75 template sets `potential lj`, `SearchType=0`, and `IfPer=0`. Its `lj.pot` is empty. This is an isolated-cluster configuration and does not require a large periodic vacuum cell for the native LJ model.
- The Java sources decompiled from `sgn.jar` (`decompiled/sgn/lj/LJBase.java`, `decompiled/sgn/energy/LJ.java`) contain all-pairs LJ routines with `epsilon=1` and a hard-coded `sigma=2.5011` in their default overload. They are evidence about those Java routines only; they do **not** establish the parameterization of the LASP executable's Fortran/native `potential lj` backend. Native backend sigma, epsilon, truncation, and force convention remain unverified here. Do not claim that native LASP differs from or matches FullPairLJ(ε=1 eV, σ=2.7 Å) based on these Java sources.
- A general fixed-cell LASP-to-ASE bridge already exists in `research/ga_ssw/lasp_external_ase.py`, documented in `docs/research/2026-09-20-lasp-ase-fixed-cell-bridge.md`. It sends LASP `external.coord` requests to a persistent ASE Calculator and returns energy and forces through `external.ene`; this is not a new integration proposal. The helper is fixed-cell E/F only and the documentation records protocol calibration with EMT, not a FullPairLJ search comparison.
- `IfPer=0` identifies nonperiodic search settings but does not guarantee that LASP's external interface preserves unwrapped cluster coordinates through an entire search. The bridge documentation records observed folding/image changes and explicitly says centering the initial structure does not guarantee correct geometry later in a long nonperiodic search. Its startup check only rejects input atoms outside the storage cell; it is not a trajectory-wide no-wrap guarantee.

## Readiness and remaining checks

An exact-potential LASP-versus-Python SSW comparison is technically plausible through the existing ASE bridge, using the same `FullPairLJ` Calculator, but it is **not ready to interpret as a nonperiodic cluster comparison** until the image/wrapping issue is resolved or the protocol is explicitly restricted to a verified domain. The bridge documentation's two proposed routes remain the relevant boundary: a separately qualified periodic domain for a periodic potential, or evidence that a native path transmits unwrapped coordinates/images. Do not infer either route is already qualified for LJ55/LJ38.

The following remain unknown from this read-only review:

1. Actual native LASP binary LJ equation and parameters (including cutoff/shift), verified against independent E/F values.
2. Whether an identical input structure can be supplied directly to the original LASP SSW run, with a controlled/recoverable random stream, rather than generated by TYPE0 population initialization.
3. Whether the existing external bridge can preserve the intended isolated-cluster geometry over the full SSW trajectory. The current fixed-cell helper and prior calibration do not establish this.

Thus: native TYPE0 LJ is available as an archived route, but its potential identity and same-start comparability are unproven; the external ASE route is implemented, but its nonperiodic long-trajectory geometry fidelity is unqualified. No route is yet established for a scientifically matched LJ55/LJ38 comparison.
