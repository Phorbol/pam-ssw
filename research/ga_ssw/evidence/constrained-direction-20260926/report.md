# 非周期受限完整方向：实现与资格结论

用户批准的最小方案已实现，保持旧默认。`run_constrained_ssw(...,
recovered_direction=...)` 使用真实全原子几何选轴，在活动笛卡尔空间生成方向、
保存阶段位移记忆与旋转；不再删除自由簇刚体运动。schema2保存方向状态及
活动空间身份，旧schema1仍可读。未加入池、周期slab、VC/RC或Q描述符。

## 验证与解释

| 体系/协议 | 结果 | 搜索请求与复核 |
|---|---|---|
| Cu13/EMT，固定三原子，连续2步 vs 1+1恢复 | 每路径6个Gaussian、2个有效落点；坐标/完整状态/RNG/成本严格一致；固定原子不动 | 476 + 6 |
| 同一Cu13 + native LS | 每路径6个Gaussian、2个有效落点，LS状态也严格一致 | 532 + 6 |
| C4H6/MH-1/omol，固定点Hookean，独立两运行 | 均完成269请求；严格比较失败，初淬火在恢复前已经不同；原失败保留 | 538 + 3 |
| 同一分子，记录真实oracle后逐查询重放 | 269次重放查询坐标完全一致，连续/恢复完整状态一致；4个Gaussian、2落点 | 269 live + 269 replay + 3复核 |

总计1815次真实搜索请求、269次重放、18次独立复核。请求数不是模型forward次数。
重放不是第二条独立物理轨迹；不以严格重放通过声称独立GPU运行逐位可复现。
所有实际复核过的保存极小值满足既定活动力0.03 eV/Å门槛；Cu普通/LS最大值
分别0.02994036/0.02289388 eV/Å。点约束仅为接口资格夹具，不推荐其防碎裂参数。
这些结果关闭状态/几何组合缺口，不能证明搜索效率、成笼率或LS优势。

最终针对性回归：CPU作业1498659，`tests.sbatch`运行两个新增测试文件、
constrained reference/rotation/Hookean/LS/LS-failure/PAM、RC/RC-VC及
native/recovered direction/control/checkpoint/driver测试，**133 passed in 2.06s**。
命令完整文件清单见verification-command.txt；原RED日志保留。
修复包含一次性索引迭代器、活动状态内部一致性、选轴失败不丢合格落点。
选轴终止失败发生在MC/LS更新前，故保留落点但不更新LS，也不支持该终态恢复。

## 可复现性与边界

物理作业基于1ec428e加`source.patch`和`constrained_direction.py`快照。
最终代码另有一次仅改变快照复制时机的修复：无磁盘输出时不逐外步复制整个
历史；显式checkpoint_path仍逐步保存。RED1498611先证实原开销，最终回归覆盖修复。
不为这项非数值改动重复消耗物理计算。原始输出在本目录runs-*，大数组/检查点
未放入Git；Git保留协议、脚本、源码快照、摘要及执行日志。

分子重放1498598原在4V100因资源排队，将同一待运行作业改为8V100V0后完成；
仍为一张V100、20分钟、原请求上限，未重复提交。没有账户或QOS故障。

后续保留缺口：受限池重启、周期选轴、VC/RC完整状态仍需独立设计；
随机C60全局验收仍未通过。本变更不扩大这些结论或自动启用新默认。
