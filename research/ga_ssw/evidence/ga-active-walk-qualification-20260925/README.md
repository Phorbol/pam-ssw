# GA 活动轨迹恢复：工程资格检查

目标是在不改变搜索方法的前提下，让 GA 的四类 SSW 轨迹可以在完整外步之后保存、跨进程继续；不是 GA 搜索效率比较。

## 首轮 Cu13/EMT：未通过，定位到落点重复入库

冻结源码 `3b8f761`，CPU1483789。协议见 [plan.md](plan.md)，执行版本见 execution.json，原始结果保留 runs/ 和 slurm-1483789.out。

连续默认与开启检查点各9250次搜索请求、39条观察。四种恢复（quick、offspring_ssw、generation_short、fine）均保持9250累计请求、完整 SSW walks 和最终 RNG 相同，但观察数变成40。自动比较在 quick-resume 拒绝通过。首轮总费用55500次EMT请求，未执行独立终态复核。

原因已由独立代码审查和真实端到端结果共同确认：GA用Python对象身份判断 minima 与 record.landing 是否是同一落点，而SSW检查点分别复制这两组数据，恢复后丢失共享身份。修复应依据现有SSW契约：所有收敛落点已在minima中，额外保留record中的不收敛落点；不按能量或几何近似合并不同观察。

另需修复保存回调的异常被搜索失败处理吞掉的问题；在明确的恢复契约下核验不匹配状态的拒绝行为。原始失败产物不覆盖；修复后使用 runs-v2，累计搜索预算仍不超过120000。后续MH1仅在EMT资格通过后执行。

## 修复后 Cu13/EMT：通过

冻结core `3c23a3a`，CPU1483815，产物 [runs-v2-analysis.json](runs-v2-analysis.json)。四种暂停/恢复均9250累计请求、39条观察；与完整默认结果的5组完整JSON比较（含最终RNG，排除检查点管理字段）全部相同。11个archive结构fresh能量匹配且fmax≤0.01 eV/Å。主agent独立重读JSON确认五组一致、累计费用闭合。

两轮合计111000搜索+11fresh，未超120000搜索上限。54项核心回归通过（CPU1483808）；随后2项失败落点/EMT-LS观察补充检查通过（CPU1483820，与54项存在重叠，不相加）。ASE给出NumPy shape弃用警告，未引发失败。测试补充提交37c1e38不改变受测core。

保留旧v1和默认完成阶段回调；新checkpoint_walk_steps显式启用外步快照，恢复共享原总预算。保存回调异常向调用者报告；不在GA中静默继续。此处证明工程恢复与力资格，不证明GA搜索优势或C60成笼。下一步是有界MH1检查。
