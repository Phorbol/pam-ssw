1467850 config-only check passed zeroPES. Static root review found recovered_rotation cannot coexist with baseline pre_rotation_hvp, so method-specific override added. 1467864 cross-option check rejected rotation_bias0: legacy SSWConfig requires positive value even though recovered branch ignores it. Corrected unused placeholder to existing100; no PES/GPU run occurred, no algorithm parameter tuning. v3 additionally checks the public driver reaches a deliberately failing oracle after all config validation; no model calls.
1467872 reached oracle sentinel as intended, but test incorrectly expected InitialQuenchError wrapper; actual Safe-total initial evaluation propagates RuntimeError unchanged. Corrected assertion to exact original sentinel. Runner/core unchanged by this correction. NoPES.
