# Status

Complete.

- C60: certified, 40 bootstrap true-quench calls plus one validation call,
  2.6780 s.
- PdO: certified, 40 bootstrap true-quench calls plus one validation call,
  2.9699 s.
- Both ledgers have zero unattributed calls.
- Neither check dispatched a proposal action.

The corrected sufficient L-BFGS-B component bound required a few more calls than
the prior premature endpoints, while preserving the original `0.03 eV/A`
per-atom certificate.
